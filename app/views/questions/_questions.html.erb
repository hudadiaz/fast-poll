<% questions.each do |question| %>
  <div class="my-1">
    <%= link_to question_path(question), class: 'btn btn-secondary w-100 text-left' do %>
      <span class="badge badge-dark"><%= question.respondents.size %></span>
      <%= question.question %>
    <% end %>
  </div>
<% end %>